import React, { FC } from "react";

const Icon: FC<{ type: string; className?: string }> = ({
  type,
  className,
}) => {
  switch (type) {
    default:
      return <></>;
  }
};

export default Icon;
